# Timestamped local job run output (harbor run / harbor job start). Ephemeral,
# host-specific (absolute paths, container ids), regenerated by anyone who
# reruns jobs/tampering.yaml or the sa-replay-feasibility task. Keep the job
# config itself (tampering.yaml) tracked.
*/
