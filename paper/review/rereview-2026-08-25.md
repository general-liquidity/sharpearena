# Re-review (verification review), 2026-08-25

Mode: re-review per `re_review_mode_protocol.md`, adapted to this repository's artifact set (no ARS contract chain exists here; criteria were taken verbatim from the two committed audits, evidence verdicts were formed against manuscript + evidence + code BEFORE weighing the post-repair report's claims, and every verdict below carries its own independently recomputed or quoted evidence). Single-family disclosure: this verification round ran on the same model family that drove the revisions; over-optimization to this judge's latent biases is possible.

Inputs: `audit-math-2026-08-25.md` (M-01..M-19), `audit-rigor-2026-08-25.md` (S/A/D/C/W/R items), `final-integrity-2026-08-25-post-repair.md` (repair claims), the current manuscript (`main.tex` + all 18 section/fragment files), `paper/evidence/*.json`, `README.md`, and code (`realism.py`, `market_making.py`, `eval_seeds`-adjacent surfaces, `scenario_gen.rs`, `make-f3/-f5/-witness/-predictability`). Recomputation used the committed venv (`crates/sharpearena-py/.venv`, numpy/scipy). Figure-layout code and the in-flux witness `noise_replicates` internals were verified for substance only, per the caller's note.

Every quantitative check that could be re-run cheaply was re-run; none was taken from the post-repair report on its word.

## Key recomputations (all independent)

1. F4 canonical counts from `f4-realism.json`: Calm 0/8, Hard 0/8, Extreme 1/8, total **1/24 pass**; per-seed `passed` equals the conjunction of exactly the three gated checks on all 48 stored reports. Table means reproduce (kurt -0.97/+16.30/+11.38; ACF +0.0002/-0.0107/-0.0118; Fano 1.32/0.93/0.91; Calm aggregation +0.57, 8/8 in every tier). Clustered (strength 0.5): 0/8, 8/8, 4/8, matching the text.
2. Calibrated ACF null in `realism.py`: `_abs_acf_iid_upper` is seeded with a fixed constant mixed with the panel geometry, simulates 20,000 IID Gaussian panels of the exact estimator geometry, and takes the 95th percentile. Recomputed threshold for (120, 4, 10) = 0.014556445930936494, byte-equal to every stored per-seed threshold in the evidence. A fresh 400-panel IID Gaussian simulation passes the gated check 28/400 = 7.0% (nominal 5%, within Monte Carlo noise), i.e. an IID panel passes the "no clustering" side about 93 to 95% of the time. The M-03 objection (uncalibrated, biased null) is resolved.
3. Aggregational Gaussianity is redefined as abs(k1) - abs(kh) > 0 in `_aggregational_gaussianity`; a platykurtic panel converging to Gaussian now passes (Calm: 8/8), resolving M-04's sign-convention artifact.
4. F5 32-seed confirmation from `f5-manipulation.json`: seeds 30000..30031 (disjoint from the 8 search seeds), one fixed pre-selected cell (theory arm, beta 0.5, 9:1 uniform); recomputed mean +26.31e-4, t interval [23.75, 28.86]e-4, df 31, critical value 2.0395, all byte-matching the stored stats and the manuscript's +26.3 [23.7, 28.9]. All 32 per-seed values are positive (sign-test p = 4.7e-10), so the conclusion is now distribution-free, exactly the remedy M-08 asked for.
5. 195-cell familywise value: recomputed t_{1-0.05/390, 7} = 6.786175237491603, equal to `BONFERRONI_T_CRIT_DF7_GLOBAL = 6.7861752374916025` in `make-f5-manipulation.py`; family size asserts 60 and 195 in source. Extension counts recomputed over all 60 stored cells: 23 familywise-positive, 31 negative, 6 crossing; largest cell 45:5 theory arm +135.0e-4, familywise [122.1, 148.0]e-4. All match the fragment and the post-repair report.
6. 135-cell grid re-verified after regeneration: best cell +21.8e-4, pointwise [17.4, 26.2]e-4, familywise [10.0, 33.6]e-4; temporary twin +18.4e-4, familywise [6.4, 30.4]e-4; all 45 linear cells negative; canonical arm has no positive mean; 1:9 vs 9:1 linear near-symmetry present. Concave arm: 7/23 slots cross zero at beta 0.5 (collapsing to 5 distinct configurations: the canonical point stored under three axes, plus eta 0, eta 0.1, push 0.75, push 1.0), 0/23 at 0.7; base cells -8.9 [-19.6, +1.8] and -13.5 [-21.2, -5.9]e-4. All match.
7. Witness: `witness.json` now carries `noise_replicates` with 5 independent noise paths per cell (noise-seed uniqueness asserted in the producer). All 12 table cells recompute exactly (e.g. sign_follow gap-band Hard mean 0.730, min 0.702, max 0.752; deadband F1-band Hard 0.621 [0.483, 0.755]); 50/50 attained coarse series monotone; the sole binding gate at every attained crossing is `per_run_gate_all_seeds`; the "moves by 0.05 to 0.27" spread and "widest for deadband on Hard" (0.272) reproduce. M-13's single-noise-path objection is resolved.
8. Band naming vs code: `make-f3-generalization.py` uses test seeds range(10016, 10032); `make-witness.py` calls `train_test_seeds(16, 16, 0, 10000)`. Section 3.4 now states the canonical bands AND the 16-seed subsets [0,16) / [10016,10032), calling the latter "a separated gap-band evaluation subset, not the canonical held-out namespace"; the witness table band column reads "gap-band witness". M-19/A2 resolved.
9. Sealed-seed modulus: `scenario_gen.rs` `sealed_seed` reduces by `span = u64::MAX - EVAL_SEED_BASE`; the fragment now displays mod (2^64 - 1 - EVAL_SEED_BASE). M-06 resolved. Coverage 3.5527e-15 and expected recoveries 5.68e-14 confirmed in `sealed-seeds.json`.
10. Kappa: the impact exponent is beta everywhere (abstract, intro, 03-env, all F5 fragments, A-commands); 03-env explicitly says the naming "avoids overloading the market-making model's kappa". C1 resolved.
11. F6 endogenous arm: sweep levels recomputed from the `endogenous` key: informed +0.385/+0.313/+0.240/+0.095/-0.050/-0.196 across lambda 0 to 0.4, matching the table and abstract; the exogenous-vs-per-episode convention is stated in the caption; level CIs are printed. M-11 resolved.
12. F8 extinction events: stored `generation: 0` for `overconfident` on both schedules; the text still says "by generation 1". Calm-calibration: 99 cells, `n_qualifying` 0, `chosen` null; nearest candidate (clustering 0.5, prob 0.02, pers 0, size 0.015) has 7/8 diagnostic, 5/8 confirmation, vol ratio 1.1497; the 8/8+7/8 alternative has ratio 1.2894. Fragment matches exactly.
13. Certified-Calm sweep: no manuscript or README text calls any Calm configuration certified; the calm-tails fragment and F4 state the negative result. Zero-issue criterion 1 of the post-repair report holds.
14. Predictability: seeds 50000..50015, band width 2^16, 16/16 recovered, 14.68 us/candidate, 8.5786e6 CPU-year extrapolation unchanged and consistent with the text.

## Traceability table

Status vocabulary: VERIFIED-FIXED / PARTIALLY-FIXED / NOT-FIXED / DISPUTED. "Repair claim" quotes or paraphrases `final-integrity-2026-08-25-post-repair.md`; "none" means that report does not claim the item was repaired.

### Math audit (audit-math-2026-08-25.md)

| ID | Sev | Repair claim | Independent verification | Status |
|---|---|---|---|---|
| M-01 | MINOR | Concave fragment "now states the normalized-flow scale caveat" | Fragment: "At fixed lambda and sub-unit x, lowering beta also raises the impact magnitude relative to the linear term, so this is a falsifiability stress test rather than a scale-matched comparison of impact shapes." In-section statement was the audit's accepted alternative to a scale-matched arm; limitations paragraph retained. | VERIFIED-FIXED |
| M-02 | MINOR | none | `market_making.py` docstrings still read "analytically optimal ... provable optimum" (lines 1, 7-8) and `analytically_optimal_policy` still says "ground-truth optimum" (215-217). Paper language is correct; code docs unchanged. | NOT-FIXED |
| M-03 | MAJOR | F4 regenerated; ACF gated at a fixed 95th-percentile IID null | Recomputation 2 above: seeded, 20,000-panel, 95th-percentile null; threshold 0.0145564... byte-matches evidence; fresh IID gate rejection ~5-7%; counts now 1/24; the -1/T misattribution sentence is gone; "calibrated" is now earned. | VERIFIED-FIXED |
| M-04 | MAJOR | Aggregation redefined toward zero absolute excess kurtosis | Recomputation 3: `abs(k1) - abs(kh)`; Calm passes 8/8 with mean +0.57; caption reports per-tier aggregation passes. Residual: the paper still does not attribute Calm's platykurtosis to the generator's uniform innovations, and the upstream `data.rs` "Gaussian-ish" comment lives in the sharpebench repo, outside this tree's repair. | VERIFIED-FIXED (small residual) |
| M-05 | MINOR | none | `realism.py:149` still `np.convolve(..., "full")[:len]`: zero-padded partial windows at series start. Informational-only statistic. | NOT-FIXED |
| M-06 | MINOR | Fragment states "the exact derived-seed modulus 2^64-1-EVAL_SEED_BASE" | Fragment displays mod (2^64 - 1 - EVAL_SEED_BASE); code `u64::MAX - EVAL_SEED_BASE`. Match. | VERIFIED-FIXED |
| M-07 | MINOR | none | F2 still reports "sixteen episodes do not resolve either point" with no minimum-detectable-effect sentence. | NOT-FIXED |
| M-08 | MAJOR | "fixed-cell 32-seed confirmation is +26.3e-4 [23.7, 28.9]e-4" | Recomputation 4: exact match; disjoint fresh band; 32/32 positive so even the sign test is conclusive (p 4.7e-10). This is precisely the audit's proposed decisive fix. The n=8 normality dependence of the familywise interval is not stated in the fragment, but the replication supersedes it. | VERIFIED-FIXED |
| M-09 | MINOR | none | "8.6 million CPU-years" still not labeled as Python-harness-specific (a native scan is 100-10,000x cheaper). Conclusion unaffected. | NOT-FIXED |
| M-10 | MINOR | "Claims are scoped to that scanner and workflow" | The fragment now prints the coverage (3.6e-15) beside 0/16 and calls the result "narrow ... this particular bounded-band enumeration budget", but does not say 0/16 was arithmetically guaranteed (a coverage identity). | PARTIALLY-FIXED |
| M-11 | MINOR | F6 endogenous table states the convention | tab:f6-endo caption states per-episode convention and that exogenous rows "differ from the pooled quantity-weighted aggregates of tab:f6 in the third decimal"; level CIs printed at every horizon (informed h20 [0.341, 0.430]). | VERIFIED-FIXED |
| M-12 | MINOR | none | "The root-level winner differs ... on 5 of 8 seeds, so shocks do reorder outcomes more often than not" retained (2 of the 5 are unresolved-control seeds; n=8 binomial noise); "overconfident by generation 1" retained while the stored events are generation 0 (recomputed, recomputation 12). | NOT-FIXED |
| M-13 | MAJOR | Witness "crossings are observed brackets, not global thresholds" | Recomputation 7: five independent noise paths per cell, table reports mean [min, max] over paths, all 12 cells reproduce, single-path over-reading explicitly warned against, bracket half-width labeled numerical resolution. | VERIFIED-FIXED |
| M-14 | MAJOR | none | tab:f3 caption still classifies the Hard-Extreme cells against "the 0.34 seed-noise calibration" and the body still says "its cells split by the paper's own noise calibration of 0.34". One realized gap is still used as a decision threshold; the CIs that do the job correctly sit in the same paragraph. | NOT-FIXED |
| M-15 | MINOR | none | tab:f1 Hard equal-weight 0.1114 (n_trials=6) vs tab:f3 0.1231 (n_trials=0), adjacent tables, still no footnote or explanation. | NOT-FIXED |
| M-16 | MINOR | none | Percentile-bootstrap boundary undercoverage for the [0,1]-bounded DSR still unnamed; no BCa. Pre-existing "coarse instrument" sentence unchanged. | NOT-FIXED |
| M-17 | MINOR | none | Pass^k monotonicity (0.62/0.31/0.06) and F7 clean-rate ordering still lack a binomial-width clause (about +-0.2 at n=16). | NOT-FIXED |
| M-18 | MINOR | none | New sentences: "All +- quantities here are across-seed standard deviations, not confidence intervals" and "No hypothesis test or equivalence margin was preregistered". The ddof convention is still unstated and the (significant) Calm paired-t edge is still unreported. | PARTIALLY-FIXED |
| M-19 | MAJOR | 03-env and witness "distinguish canonical seed namespaces from the 16-seed gap-band instantiation" | Recomputation 8: 03-env names the canonical bands and the actual 16-seed subsets, explicitly "not the canonical held-out namespace"; preamble scoping statement 3 repeats it; witness band column renamed; verified against producer code. | VERIFIED-FIXED |

### Rigor audit (audit-rigor-2026-08-25.md)

| ID | Sev | Repair claim | Independent verification | Status |
|---|---|---|---|---|
| S1 | MAJOR | none | Preamble now reads "Ten findings, eight core calibrations plus two follow-up probes, address questions left open ...: an existence witness ..., an asymmetric positive control ... and sealed evaluation seeds". "Two follow-up probes" precedes a list of three, and Section 5 now has 15 subsections (concave, extended sweeps, calm-tails, endogenous arm, predictability are uncounted). The mapping is still not legible and the count is now internally inconsistent (see New issue N1). | PARTIALLY-FIXED |
| S2 | MAJOR | none | Follow-ups are still interleaved; each is now introduced by its parent (F1 forward-references the witness, F5 its three extensions, F6 the endogenous arm), which mitigates the spliced feel, but no sentence states the placement rule. | PARTIALLY-FIXED |
| S3 | MAJOR | README "removes revision-relative ... language" (README only) | Swept the manuscript: "in this revision", "earlier draft", "old public band", "can now bind", "Two named earlier", "byte-identical to their previous values" are all gone. Residuals: abstract still says "overturns its original single-seed headline" (the audit's named instance), and A-commands says "the regenerated evidence". | PARTIALLY-FIXED |
| S4 | MAJOR | none | Provenance manifest upgraded (`repository_base_head` fa494389..., source snapshot hash, "content snapshot, rather than a guessed package label"). But the statement is still triplicated (02-principles end, 05 preamble, A-commands opening) and no Git hash is printed anywhere in the text. | PARTIALLY-FIXED |
| S5 | MINOR | "keeps the 23/24 F4 ... values aligned" | Number is consistent everywhere (23/24 fail = 1/24 pass, six sites checked). Noun still varies: "seeded panels" (abstract, intro, 03-env), "runs" (05 preamble), "canonical runs" (limitations). | PARTIALLY-FIXED |
| S6 | MINOR | none | The not-searched list now appears once in the positive-control fragment; limitations paraphrase briefly; the new extension fragment actually sweeps several of the previously unsearched axes. | VERIFIED-FIXED |
| S7 | MINOR | none | Intro still: "Cross-regime transfer costs the Calm-selected reference nearly its entire DSR", without the Calm-to-Hard caveat (interval reaching 0.054). | NOT-FIXED |
| A1 | MAJOR | Abstract "identifies native/WebAssembly determinism rather than falsely saying Python bindings are pinned" | Abstract verified: "pinned byte-for-byte across native and WebAssembly builds and exposed through Python bindings". Limitations paragraph 5 is explicit that no Python golden exists. Residual: `04-contract.tex` still says "the Rust core, its WebAssembly and Python bindings ... are pinned to the byte", contradicting both; no Python-side golden-fingerprint test was added (checked the Python test tree; `test_adverse_selection.py` goldens are Python-local, not native cross-checks). | PARTIALLY-FIXED |
| A2 | MAJOR | Canonical namespaces distinguished from the 16-seed instantiation | Same evidence as M-19; band ranges verified against `make-f3` and `make-witness`. | VERIFIED-FIXED |
| A3 | MAJOR | Witness text "calls the latter a gap-band witness subset, not the canonical held-out namespace" | Three surfaces are now named and used consistently: canonical bands, gap-band evaluation subset, evaluation namespace with its named held-out regression set; the sealed fragment's "public held-out set" now unambiguously means the namespace set. Residual: contribution 5 still says "keeping the disjointness proof public" (elsewhere "by construction"). | VERIFIED-FIXED (small residual) |
| A4 | MAJOR | "F2, F5 and F6 use market-model processes that are not themselves submitted to F4" (stated as a deliberate limitation) | New limitations paragraph "The realism diagnostic does not certify every experiment's process" says exactly this, in both directions. But the Section 5 preamble's first scoping statement still blankets all findings as calibrations "on a generator whose canonical tape ... declines to certify", and F2, F5, F6 carry no one-line qualifier at point of use. | PARTIALLY-FIXED |
| A5 | MINOR | none | Still three names: "volatility-persistence driver" (abstract), "vol_clustering knob adds deterministic EMA persistence" (F4), "weak volatility clustering" (limitations). | NOT-FIXED |
| A6 | MINOR | none | "Dispersion is reported throughout" retained; the sentence now enumerates F1/F3/F2/F5/F6 conventions but does not flag F4, F7, F8 as count-based. | PARTIALLY-FIXED |
| A7 | MINOR | none | Contribution 3 still "making generalization a measured quantity"; limitations still correctly say no learner has been measured. | NOT-FIXED |
| D3 | MAJOR | 07-limitations "removes a stale assertion about a current companion window and specifies the operational conditions" | Verified: the "currently open companion window" sentence is gone; limitations now state "Any public bounded-band evaluation should be treated as compromised by this scanner until it documents sealed seed custody, a pre-entry commitment, non-reuse and a reveal deadline". The disclosure exists (generalized rather than naming the companion's window), which satisfies the audit's substance. | VERIFIED-FIXED |
| D4 | MINOR | none | tab:f4 caption now states aggregation pass counts per tier (8/8 each) and the gated conjunction explicitly includes it; Calm mean given in text. | VERIFIED-FIXED |
| D5 | MINOR | "report Fano construction" | The calibration construction is now stated (conditional on observed exceedances, Bessel-corrected sample Fano over its exact equal-block IID expectation), but the base ratio (variance-to-mean of counts) is still never defined at first mention. | PARTIALLY-FIXED |
| D6 | MINOR | Abstract "expands PSR once" | Verified: abstract expands "probabilistic-Sharpe-ratio (PSR)"; witness fragment defines it at first use ("the probability under the kernel's stated sampling model that Sharpe exceeds its reference threshold"), attributing the gate to the kernel spec, the audit's accepted alternative to citing Bailey and Lopez de Prado 2012. | VERIFIED-FIXED |
| D7 | MINOR | none | The fragment now identifies the winner as the no-follower, zero-temporary-impact arm, states the canonical arm has no pointwise-positive cell before the "instrument can detect" conclusion, and limitations echo "with no followers". | VERIFIED-FIXED |
| D8 | MINOR | none | No "DSR is a probability capped at 1" clause in F1; only "saturate the DSR at 1.0000". | NOT-FIXED |
| C1 | MAJOR | (implied by beta usage in the frozen-evidence table) | Exponent renamed beta throughout (abstract, intro, 03-env, fragments, A-commands, figure producers via `impact_exponent`); 03-env states the rename rationale explicitly. Kappa is now only the A-S arrival-decay parameter. | VERIFIED-FIXED |
| C2 | MINOR | none | s is still the mid price in the A-S formula (03-env) and the signal-truth correlation in the witness, which precedes F2 in reading order. | NOT-FIXED |
| C3 | MAJOR | Fragments "now state ... figure references" | All four previously orphaned figures are now \cref'd in their fragments (fig:witness, fig:f5-concave, fig:f5-positive-control, fig:predictability); the new figures (calm calibration, extended sweeps, f6-endogenous) are also referenced. | VERIFIED-FIXED |
| C4 | MINOR | none | Witness title/text still "acceptance set"; F1 and limitations "eligibility set"; README "eligibility region". No synonym definition. | NOT-FIXED |
| C5 | MINOR | none | Grep counts: Avellaneda--Stoikov 5 vs Avellaneda-Stoikov 4; Almgren--Chriss 1 vs Almgren-Chriss 3. Still mixed. | NOT-FIXED |
| C6 | MINOR | "No table relies on ditto cells" (adjacent) | tab:f7 now carries an explicit Bankrupt column (all zeros) and the caption states no episode reaches bankruptcy or a cascade wipe; the cascade omission is noted, which was the audit's accepted alternative. | VERIFIED-FIXED |
| C7 | MINOR | "The cited SharpeBench companion version is intentionally not changed" | refs.bib still Version 0.8.0; text still 0.5.0 kernel and 0.9.0 package with no reconciling clause. Deliberate deferral, but the reader-facing gap the audit described is unchanged. | NOT-FIXED (declared deliberate) |
| C11 | MINOR | none | 3.4 now names the evaluation namespace and the named held-out regression set, but still does not cross-reference the sealed derivation as the recommended held-out mode for adversarial settings. | PARTIALLY-FIXED |
| C12 | MINOR | none | Limitations paragraph 2 now says "defeats this specific bounded-band scanner"; paragraph 4 still says sealed seeds "remove the bounded-band attack". | PARTIALLY-FIXED |
| C13 | MINOR | none | Intro/fragment: "two selected beta=0.5 cells"; limitations: "finds one that pays at beta=0.5 with no followers" and "a profitable sampled round trip". Count drift persists; see also New issue N7. | PARTIALLY-FIXED |
| W2 | MINOR | none | "honest" count 12 -> 10; F8's "honest conclusion" became "The conclusion is methodological"; the predictability uses are the technical adversary label; "the honest zero" remains in F1 and limitations. | PARTIALLY-FIXED |
| W3 | MINOR | none | "The lesson is sharp" removed. Remaining: "worst citizen", "regardless of the weather", "we report that plainly", "with the same candor as F4", "The minimum deserves its interval", "is worth stating" (twice). | PARTIALLY-FIXED |
| W4 | MINOR | none | Abstract is now ~353 words (was 281): the repair added the confirmation, extension, endogenous-arm and witness-replication results to it. Moves away from the Round 1 should-fix. | NOT-FIXED (regressed) |
| W5 | MINOR | none | The three multi-message paragraphs (F1 reading, F3 matrix, predictability close) are structurally unchanged. | NOT-FIXED |
| W6 | MINOR | none | Title unchanged (recorded operator instruction); abstract still opens "SharpeArena is a point-in-time ..." against the definite-article title. | NOT-FIXED (operator-held) |
| R1 | MINOR | none | `almgren2005direct` present at refs.bib:214, cited nowhere in any .tex. | NOT-FIXED |
| R2 | MINOR | none | No SplitMix64 (Steele/Lea/Flood) citation; no PSR 2012 citation. The look-ahead clause was reworded so bailey2014deflated now attaches to "the selection bias the deflation literature centers", fixing sub-item (iv). | PARTIALLY-FIXED |
| R3 | MINOR | "version locator must be updated only after the corresponding SharpeBench release" | refs.bib:132-138 unchanged: GitHub URL, "Preprint", "Version 0.8.0", no arXiv/DOI. | NOT-FIXED (declared deliberate) |
| R4 | MINOR | none | `jerome2023mbtgym` title still "Mbt-gym"; eprint-in-inproceedings entries unchanged. | NOT-FIXED |

## Counts by status

- Total findings traced: 58 (math 19, rigor 39). None skipped.
- VERIFIED-FIXED: 18 (M-01, M-03, M-04, M-06, M-08, M-11, M-13, M-19; S6, A2, A3, D3, D4, D6, D7, C1, C3, C6)
- PARTIALLY-FIXED: 17 (M-10, M-18; S1, S2, S3, S4, S5, A1, A4, A6, D5, C11, C12, C13, W2, W3, R2)
- NOT-FIXED: 23 (M-02, M-05, M-07, M-09, M-12, M-14, M-15, M-16, M-17; S7, A5, A7, D8, C2, C4, C5, C7, W4, W5, W6, R1, R3, R4)
- DISPUTED: 0
- By severity: of the 15 MAJOR findings, 11 VERIFIED-FIXED, 3 PARTIALLY-FIXED (S1/S2-class structure, A1, A4; S3/S4 also partial), 1 NOT-FIXED (M-14). Every statistically load-bearing MAJOR (M-03, M-04, M-08, M-13, M-19, A2/A3, C1, C3, D3) is independently confirmed repaired. The post-repair report's seven zero-issue criteria all hold as stated; its per-surface evidence claims (1/24, 32-seed interval, 195-family counts, calm-calibration near-misses, witness split) all reproduce exactly.

## Residual list (concrete fixes, ordered by importance)

1. M-14: delete the 0.34 device from the tab:f3 caption and body; classify transfer cells solely by whether the seed-paired CI excludes zero (the text already does this in the same paragraph). One caption edit plus two sentence edits.
2. A1 residual: edit `04-contract.tex` "the Rust core, its WebAssembly and Python bindings ... are pinned to the byte" to match the abstract/limitations narrowing ("the Rust core and its WebAssembly build are pinned; the Python bindings call the same compiled engine"), or add a Python test recomputing the committed native FNV-1a fingerprints.
3. S1/S2: fix the preamble count ("two follow-up probes" vs three listed; 15 actual subsections) with an explicit map, e.g. "eight numbered findings F1-F8; seven attached follow-ups, each placed directly after its parent: witness (F1), Calm calibration (F4), concave/positive-control/extended sweeps (F5), endogenous arm (F6), sealed seeds (predictability)".
4. A4 residual: split the preamble's first scoping statement into tier-generator experiments (F1, F3, F4, F7, F8, witness, predictability) vs market-model experiments (F2, F5, F6), pointing at the existing limitations paragraph.
5. M-12: recount F8 as "3 of 6 resolved pairs", drop "more often than not", and change "generation 1" to "generation 0" (stored events are generation 0).
6. M-15: one footnote reconciling 0.1114 (n_trials=6) vs 0.1231 (n_trials=0).
7. M-02: align `market_making.py` docstrings with "closed-form reference policy".
8. S3/S4 residuals: drop "overturns its original single-seed headline" from the abstract (state it as the replication result it is), drop "the regenerated evidence" in A-commands, keep one provenance statement and print the short hash fa494389.
9. Cheap MINOR sweep: M-07 (one MDE sentence), M-09 (label the harness dependence), C2/C4/C5 (notation, set naming, en-dashes), D8, A5, A7, S7, R1/R2 (cite almgren2005direct in the concave fragment; add Steele et al. for SplitMix64), R4.
10. W4: the abstract grew to ~353 words; compress the F2, concave, endogenous and witness sentences to clauses.

## New issues introduced by the repair

- N1 (moderate): the Section 5 preamble's revised counting sentence is internally inconsistent: "eight core calibrations plus two follow-up probes" is immediately followed by a list of three follow-ups, and five further follow-up subsections (concave, extended sweeps, calm-tails, endogenous arm, predictability) are not counted at all. The pre-repair sentence at least matched its own arithmetic.
- N2 (moderate): `A-commands.tex`'s witness paragraph is now stale: it describes "one seeded standardized noise path reused at every strength" and "any bracketed threshold", but the shipped `make-witness.py` runs five noise paths per cell and serializes `noise_replicates`, which is what tab:witness now reports. The appendix contradicts the fragment it documents.
- N3 (minor): Appendix A no longer lists every artifact the paper uses: the F4 command paragraph omits the calm-calibration sweep and `f4-calm-calibration.pdf`; the F6 paragraph omits the endogenous key and `f6-endogenous.pdf`. This undercuts the appendix's "commands that produce every number" premise.
- N4 (minor): unit mismatch in the witness headline across documents: abstract, intro and README say the PSR conjunction is last to open "at all ten observed/attained thresholds/boundaries", while the fragment and its caption count "all 50 attained crossings" (10 cells x 5 noise paths). Both are true; the differing units are unexplained.
- N5 (minor): README's witness bullet retains the pre-repair primary-path number ("a turnover-rationing variant passes from strength 0.90") where the paper's table now reports 0.848 [0.777, 0.930] over five noise paths; the README also quotes per-bar cost numbers (14 bp, 2.7 bp) that appear nowhere in the manuscript or evidence summary text I could locate.
- N6 (minor): the regenerated 135-cell grid now contains FOUR familywise-positive cells (theory and temporary arms at both 8:2 and 9:1; recomputed: +21.8, +10.9, +18.4, +8.9e-4, familywise lower bounds +10.0, +2.1, +6.4, +0.0), while the figure caption says "Two sampled arms, not one, retain positive familywise intervals" (true at arm level) and the intro says "two selected cells" (true of the selected pair). No statement is false, but three granularities of the same count now coexist; one clarifying clause in the fragment would close it.
- N7 (minor): S5's noun drift gained a new member: the preamble now says "1 of 24 runs" while abstract/F4 say "seeded panels" and limitations "canonical runs".

## Summary (12 lines)

1. All 58 audit findings traced; every repair claim independently re-verified against manuscript, evidence JSONs and code; nothing rubber-stamped.
2. Counts: 18 VERIFIED-FIXED, 17 PARTIALLY-FIXED, 23 NOT-FIXED, 0 DISPUTED; 11 of 15 MAJORs fully fixed.
3. F4 is genuinely repaired: seeded 20,000-panel 95th-percentile ACF null reproduces to the byte (0.0145564), fresh IID panels pass ~93-95%, abs-kurtosis aggregation fixes the Calm sign artifact, and the 1/24 (23/24 fail) headline recomputes exactly.
4. F5 is genuinely repaired: the 32-fresh-seed fixed-cell confirmation recomputes to +26.31e-4 [23.75, 28.86], 32/32 positive (sign p 4.7e-10), removing the n=8 normality dependence; the 195-cell familywise constant (6.78618) and extension counts 23/31/6 with the +135.0e-4 [122.1, 148.0] maximum all reproduce.
5. The witness is repaired: five noise paths per cell, all 12 mean [min, max] table cells recompute exactly, 50/50 monotone, and the every-seed PSR gate is the sole binding gate at every crossing.
6. Band naming (M-19/A2/A3), the kappa-to-beta rename, the sealed-seed modulus, figure references, and the broader-impact scanner disclosure are all verified fixed; no text calls any Calm configuration certified.
7. The one unrepaired MAJOR is M-14: the 0.34 single-draw "seed-noise calibration" still classifies F3 transfer cells in caption and body.
8. A1 is only partially repaired: the abstract and limitations are correctly narrowed, but `04-contract.tex` still claims the Python bindings are "pinned to the byte" and no Python golden test exists.
9. Structure/prose MAJORs (S1-S4, A4) are partially repaired; most MINOR statistical-presentation items (M-07, M-09, M-12, M-15, M-16, M-17, D8) are untouched.
10. The repair introduced seven new issues, the two notable ones being the preamble's two-vs-three follow-up count garble and the now-stale single-noise-path witness description in Appendix A.
11. Cross-document checks: README matches the abstract on every shared number except a stale witness threshold (0.90 vs the new 0.848 [0.777, 0.930]); the companion citation remains at 0.8.0 by declared intent.
12. Verdict: the statistical substance of both audits is repaired and independently confirmed; remaining work is one MAJOR (M-14), one contradiction (04-contract Python pinning), the Appendix A staleness, and a sweep of unaddressed MINORs.
