# Rendered visual QA — 2026-08-26

The 46-page PDF was rebuilt from scratch and every page was rendered at 180 DPI for visual inspection. Four contact sheets covered the complete document; pages 16, 18, 19, and 35 were also inspected at full rendered resolution because they contain the calibration sweep, the two full-width manipulation figures, and the enlarged ecology figure.

Result: pass. The manipulation boundary and size-response panels are separate full-width floats on adjacent pages, so neither is silently dropped or shrunk. The ecology panels fit together at their enlarged size. All panels, legends, captions, axes, tables, and page numbers are present. No oversized floats, cropped objects, overlapping body text, broken glyphs, or visually apparent margin collisions were found. The build log independently reports zero overfull boxes and zero undefined references or citations.

Inspection artifacts were generated outside the repository and are intentionally not committed.
