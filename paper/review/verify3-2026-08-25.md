# Round-3 closure verification, 2026-08-25

Mode: re-review per `re_review_mode_protocol.md`, adapted to this repository's artifact
set. Criteria were taken verbatim from `rereview-2026-08-25.md` (the round-2 statuses and
its seven new issues), evidence verdicts were formed against the current tree BEFORE
weighing the closure commit's own message, and every verdict below carries independently
recomputed or quoted evidence. No file in the tree was modified by this review.

Single-family disclosure: this verification round ran on the same model family that drove
the revisions; over-optimization to this judge's latent biases is possible (Ren et al.
2026, arXiv:2607.13104 section 8.1.2).

Subject: closure claims in `c4b5785` ("paper: close the re-review residuals and pin the
Python golden claim"). Tree state: HEAD at `d93d90f` (release v0.15.0), working tree
clean at review time. Recomputation used the committed venv at
`crates/sharpearena-py/.venv/Scripts/python.exe` (numpy, scipy).

Scope: every item round 2 recorded as PARTIALLY-FIXED, NOT-FIXED or NEW. W4 (abstract
length) and the page-length items are skipped per the operator's dismissal. M-05, C7, R3,
W5 and W6 are recorded DECLINED-WITH-REASON and checked only for whether the paper
discloses the underlying limitation.

## Verdict vocabulary

CLOSED (the round-2 gap is repaired and the repair verifies), STILL-OPEN (unchanged or
insufficiently changed), REGRESSED (worse than round 2), DECLINED (operator-declined;
disclosure checked only), UNVERIFIED (not checkable in this pass; what remains is named).

## Status table

### Math audit residuals

| ID | R2 status | R3 verdict | Evidence |
|---|---|---|---|
| M-02 | NOT-FIXED | CLOSED | `market_making.py` module docstring, `_optimal_depths`, `analytically_optimal_policy` and `mm_regret` docstrings all now read "closed-form reference" / "fixed analytical reference" and add "not a proven optimum for this env's reward". "analytically optimal", "provable optimum" and "ground-truth optimum" are gone from the prose. Residual: the exported symbol is still named `analytically_optimal_policy` (see new issue X-4). |
| M-05 | NOT-FIXED | DECLINED | `realism.py:149` still `np.convolve(fine, kernel, mode="full")[: len(fine)]`. Disclosure check: the affected timescale-asymmetry statistic is labeled informational and ungated in 03-env and in the F4 paragraph, so no gated conclusion depends on it, but the zero-padded leading windows are nowhere disclosed as such. |
| M-07 | NOT-FIXED | CLOSED | F2 now carries the minimum-detectable-effect sentence: "sixteen paired episodes detect a mean regret of about 12 reward units at 80% power and a two-sided 5% level, roughly a third of the 36.6 measured at a half-spread of 2.0". Recomputed from `f2-regret.json`: paired-design MDE at the 0.5 point is 12.33 and at the 1.0 point 12.55 reward units, using t(0.975,15)=2.1314 and t(0.80,15)=0.8662 with sd 16.453 and 16.743 (both ddof 1, matching the stored `std`). 12.33/36.63 = 0.337. |
| M-09 | NOT-FIXED | CLOSED | Predictability fragment now reads "Extrapolating this Python harness's measured per-candidate cost ... The figure is harness-specific: a native scanner would be orders of magnitude cheaper per candidate, which moves the number but not the conclusion". Stored `full_u64_extrapolated_years` 8578588.93 and `per_candidate_us` 14.6757 match "8.6 million" and "14.7 us". |
| M-12 | NOT-FIXED | CLOSED | "more often than not" is gone. Text now reads "differs between the two schedules on 5 of 8 seeds, but on 2 of those the control side never resolves a dominant lineage at all; among the 6 seeds where both schedules resolve one, the winner differs on 3", and "overconfident extinct at generation 0". Recomputed from `f8-ecology.json` `multi_seed`: root winners differ on seeds 0,4,5,6,7 (5 of 8); control winner is `flat` (unresolved) on seeds 4 and 7; 3 of the remaining 6 differ. Distributions 4/2/2 and 7/1/0 match `tab:f8`. Stored extinction generation for `overconfident` is 0. |
| M-14 | NOT-FIXED (the one unrepaired MAJOR) | CLOSED | `grep 0.34` over `sections/` and `README.md` returns no F3 hit; the only 0.34x strings left are witness table cells. `tab:f3` caption now reads "Cells are read only through their seed-paired bootstrap intervals, and the Hard-Extreme cells (+-0.096), whose interval straddles zero, are not interpreted." Body: "every cell below is classified by one rule: whether its seed-paired bootstrap interval excludes zero." Quoted intervals verified against `f3-generalization.json`: Calm to Hard 0.8769 [0.0540, 0.9989], Calm to Extreme 0.9731 [0.6348, 0.9995], Hard to Extreme 0.0962 [-0.2176, 0.9113], Extreme to Hard -0.0962 [-0.8898, 0.2201] (both directions straddle zero, so the caption's +-0.096 is right), Hard train band 0.1231 [0.0000, 0.9435], Hard test band 0.4618 [0.0367, 0.9477]. One rounding nit: the Calm-to-Extreme upper bound 0.9995 is printed 0.999 (truncation, not rounding). |
| M-15 | NOT-FIXED | CLOSED (one sub-claim UNVERIFIED) | F3 body now reconciles the two tables in place: "Hard's train-band DSR of 0.1231 here and tab:f1's 0.1114 for the same policy on the same sixteen seeds differ only in the declared trial count the deflation prior consumes, six declared policies in F1 against no declared search in the gap metric". The two stored values are confirmed (`f3-generalization.json` 0.12305180817549216, `tab:f1` 0.1114). UNVERIFIED: the appended claim "rescoring the committed F3 return series at six trials reproduces 0.1114 exactly" was not independently rescored in this pass; it requires driving the SharpeBench kernel over `per_seed_returns` at `n_trials=6`. |
| M-16 | NOT-FIXED | CLOSED | F1 now states the boundary problem by name: "The deflated Sharpe is a probability, bounded in [0,1], so 1.0000 is its ceiling rather than an unbounded score, and the percentile bootstrap undercovers at that boundary: the saturated intervals are indicative, and no bias-corrected variant is applied." |
| M-17 | NOT-FIXED | CLOSED (with an arithmetic nit) | F1: "Each rate is 16 Bernoulli seeds, so near a rate of one half a single rate carries a nominal 95% half-width of about 0.24, and a difference between two of them about 0.33". F7: "Each rate is 128 episodes, so a difference between two tiers carries a nominal binomial 95% half-width of about 0.12; the episodes sharing a seed or a policy are not independent, so that width is a floor." Recomputed: single rate at p=0.5, n=16 gives 1.96*sqrt(0.25/16)=0.245; F7 difference at p=0.5, n=128 gives 1.96*sqrt(2*0.25/128)=0.1225. The F1 difference figure does not follow from the stated p=1/2 convention (see new issue X-3). |
| M-18 | PARTIALLY-FIXED | CLOSED | Both residuals are addressed in the predictability fragment: the ddof convention is now explicit ("across-seed standard deviations over the 16 seeds, computed with the population convention (ddof 0), not confidence intervals") and the significant Calm paired-t is reported rather than suppressed: "a paired t-test of the ridge AR against the baseline over the same 16 Calm seeds gives t(15) = 3.36, p = 0.004 ... the same test is inconclusive on Hard (p = 0.08) and Extreme (p = 0.40)". Recomputed from `predictability.json` per-seed accuracies: Calm t=3.36 p=0.0043, Hard t=1.87 p=0.0811, Extreme t=0.87 p=0.3961. Reported means and ddof-0 spreads reproduce exactly (55.4 +- 2.8 vs 54.1 +- 2.4; 51.2 +- 3.3; 50.6 +- 2.1), as do the MSE pairs (1.24e-4 vs 1.22e-4; 1.27e-3 vs 1.17e-3). |
| M-10 | PARTIALLY-FIXED | CLOSED | Sealed-seeds fragment now states the coverage identity: "Under a uniform-seed approximation, that scanner covers about 3.6e-15 of the derived band, so its expected number of recoveries over 16 slots is 5.7e-14: the 0/16 outcome is what the coverage arithmetic already implies, and the run verifies the implementation rather than testing an uncertain hypothesis." That is exactly what the audit asked for. |

### Rigor audit residuals

| ID | R2 status | R3 verdict | Evidence |
|---|---|---|---|
| S1 | PARTIALLY-FIXED | CLOSED | The preamble's arithmetic now checks out. "This section reports ten findings across fifteen subsections. The ten findings are the eight core calibrations F1 through F8 plus two standalone probes ... The remaining five subsections are follow-up analyses." Counted `\subsection` in Section 5: F1 to F8 (8) plus witness, predictability (2) plus concave, positive control, extended sweeps, endogenous arm, sealed seeds (5) = 15 exactly. `calm-tails` is a `\paragraph`, and the text correctly calls it "the Calm calibration paragraph" rather than counting it as a subsection. |
| S2 | PARTIALLY-FIXED | CLOSED | The placement rule is now stated ("every subsection sits directly after the finding it extends") and mapped item by item. Verified against the actual `\input` order in `05-experiments.tex`: witness at line 46 (after F1 at 7, before F2 at 48), calm-tails 137 (after F4 at 103, before F5 at 139), concave/positive-control/sweeps 189-193 (before F6 at 195), endogenous 230 (before F7 at 232), predictability 305 and sealed-seeds 307 (after F8 at 264). Every claim in the map is true of the file. |
| S3 | PARTIALLY-FIXED | CLOSED | Swept the manuscript and README for every phrase round 2 named: "overturns", "original single-seed", "regenerated evidence", "earlier draft", "in this revision", "previous values", "old public band". Zero hits in any `.tex` or in `README.md`. |
| S4 | PARTIALLY-FIXED | CLOSED (statement), see new issue X-1 | `A-commands.tex` now says "This is the paper's single provenance statement" and prints the short revision `fa494389`; 02-principles points to it rather than restating it. The revision hash appears in exactly two places (`05-experiments.tex:3` and `A-commands.tex:10`), so the closure message's "stated once" is one restatement generous, but the triplication the audit flagged is gone. The manifest's substance is a separate and worse problem: see X-1. |
| S5 | PARTIALLY-FIXED | CLOSED | Noun drift resolved. "seeded panels" is now the noun at all nine sites (abstract, intro, 03-env, 05 preamble, F4 body, limitations, README twice), and "runs" / "canonical runs" no longer appear in this role. This also closes N7. |
| S6 | VERIFIED-FIXED in R2 | not re-opened | Carried forward. |
| S7 | NOT-FIXED | CLOSED | Intro now reads "Cross-regime transfer costs the Calm-selected reference nearly its entire DSR on Extreme, decisively so; the Calm-to-Hard cell is positive throughout its interval but reaches down to 0.054, so only the Extreme collapse is resolved at 16 seeds." The 0.054 matches the stored lower bound 0.05404852610101361. |
| A1 | PARTIALLY-FIXED | CLOSED (with X-2) | The contradiction is resolved on both sides. `04-contract.tex` now claims only "the Rust core, its WebAssembly build and the scenario bytes returned through the Python bindings are pinned to the byte"; limitations paragraph 5 narrows the Python coverage to "the scenario serialization, not the package's other serialized artifacts". The proof now exists: `crates/sharpearena-py/tests/test_python_golden.py` asserts all seven committed goldens through two new pyo3 entry points. All seven constants match `scenario_gen.rs` byte-for-byte (calm b7cf976c71219c52, hard 2ef5aff1a71605e6, extreme b0820c4d2c737f88, cointegrated a3d227424ef05868, regime 8b822cf3c9d3038f, hard-clustered a1d231f7e114a381, calm-calibration-candidate 32a63f8e5743ec93). Ran the suite: 8 passed. The test recomputes FNV-1a in pure Python with no shared helper, so it is an independent check rather than a tautology. See X-2 for the "all three runtimes" wording. |
| A2, A3 | VERIFIED-FIXED in R2 | not re-opened | Carried forward. |
| A4 | PARTIALLY-FIXED | CLOSED | Scoping statement 1 is now split at point of use: "the findings that run on the canonical position-trading generator, F1, F3, F4, F7, F8 and the witness and predictability probes, are instrument calibrations on a tape that the ... realism diagnostic ... declines to certify ...; F2, F5 and F6 instead run on the market-making and market-clearing models, which are separate simulators with their own structural and positive-control checks and are not submitted to that diagnostic, as sec:limitations states." The limitations paragraph it points at exists and says the same in both directions. |
| A5 | NOT-FIXED | CLOSED | "volatility-persistence driver" is gone. The driver is called "volatility-clustering driver" in both the abstract and F4; "weak volatility clustering" in limitations describes the tape property, not a third name for the knob; "EMA persistence" appears once as the mechanism. |
| A6 | PARTIALLY-FIXED | CLOSED | The preamble now enumerates all three conventions including the count-based ones: "F1 and F3 use 2,000 seed bootstrap resamples, F2, F5 and F6 use t-based 95% intervals over committed vectors, and F4, F7 and F8 are count-based, reporting pass counts, episode counts and seed-level winner distributions rather than interval estimates." |
| A7 | NOT-FIXED | CLOSED | Contribution 3 is now qualified in place: "making the generalization gap a measured quantity for any policy submitted to it, though no learner is measured here (cref sec:limitations)". |
| D3, D4, D6, D7 | VERIFIED-FIXED in R2 | not re-opened | Carried forward. |
| D5 | PARTIALLY-FIXED | CLOSED | The base ratio is now defined at first mention, in 03-env (which precedes Section 5): "an exploratory Fano exceedance-count ratio (the variance-to-mean ratio of the number of large-return exceedances per equal-length block, which is 1 for an IID Poisson-like count and above 1 when exceedances bunch)". |
| D8 | NOT-FIXED | CLOSED | See M-16: the "DSR is a probability, bounded in [0,1]" clause is now in F1. |
| C2 | NOT-FIXED | CLOSED | The A-S formula in 03-env now uses capital `S` for the mid price ("reservation price r = S - q gamma sigma^2 tau ... around the mid price S"), so lowercase `s` is unambiguously the witness signal-truth correlation. |
| C4 | NOT-FIXED | CLOSED | "acceptance set" is gone from the manuscript; "eligibility set" is the single name at all seven sites (F1 twice, limitations, witness fragment three times, README). "eligibility region" no longer appears in README. |
| C5 | NOT-FIXED | CLOSED (manuscript) | Zero single-hyphen "Avellaneda-Stoikov" or "Almgren-Chriss" in any `.tex`; en-dash form used at 9 and 4 sites respectively. README still uses hyphens, where the typographic distinction is moot in plain Markdown. |
| C7 | NOT-FIXED (declared deliberate) | DECLINED, and now stale (X-5) | `refs.bib` moved from Version 0.8.0 to Version 0.9.0. Disclosure check: `A-commands.tex` does disclose the version spread ("F1 serializes sharpearena 0.9.0; several other evidence files do not serialize a runtime version ... and where present they record SharpeBench 0.5.0"), which is the reader-facing gap the audit described, partially closed. But the citation is now behind the companion's actual release: see X-5. |
| C11 | PARTIALLY-FIXED | CLOSED | 03-env now cross-references the sealed derivation as the recommended adversarial mode: "All of these bands are public and therefore enumerable; for an adversarial evaluation the recommended mode is the sealed derivation of cref sec:sealed-seeds, which keeps the band structure checkable while hiding the concrete seeds behind a salt." |
| C12 | PARTIALLY-FIXED | CLOSED | Both limitations paragraphs now say the same scoped thing: "defeats this specific bounded-band scanner 0/16 when its salt is withheld" and "sealed seeds ... defeat this specific bounded-band scanner but do not answer this one". The unscoped "remove the bounded-band attack" is gone. |
| C13 | PARTIALLY-FIXED | CLOSED | The count is now consistent across granularities. Limitations reads "finds cells that pay at beta = 0.5 with no followers" (plural, arm-agnostic), intro reads "two selected beta=0.5 cells", and the positive-control fragment explicitly separates all three granularities. See N6. |
| W2 | PARTIALLY-FIXED | STILL-OPEN | "honest" count in `sections/` is unchanged at 10 since round 2 (abstract 1, intro 2, 03-env 1, F1 1, limitations 1, predictability 4). Most remaining uses are the technical adversary label, but "the honest zero" survives in F1 and limitations. Never claimed fixed by the closure. |
| W3 | PARTIALLY-FIXED | STILL-OPEN (substantially reduced) | Of the six phrases round 2 named, four are gone ("worst citizen", "regardless of the weather", "we report that plainly", "with the same candor as F4"). Two remain: "is worth stating" (02-principles) and "The minimum deserves its interval" (F2). |
| W4 | NOT-FIXED (regressed) | SKIPPED | Abstract length, dismissed by the operator. |
| W5 | NOT-FIXED | DECLINED | Multi-message paragraph structure. No disclosure obligation attaches to a prose-density preference. |
| W6 | NOT-FIXED (operator-held) | DECLINED | Title unchanged at `main.tex:31` ("SharpeArena: The Point-in-Time (PIT) RL Environment for Trading Agents"); abstract still opens "SharpeArena is a point-in-time ...". Recorded operator instruction; no disclosure obligation. |
| R1 | NOT-FIXED | CLOSED | `almgren2005direct` is now cited in `concave-fragment.tex:3`. The refs.bib entry at line 199 is no longer orphaned. |
| R2 | PARTIALLY-FIXED | CLOSED | The SplitMix64 sub-item is closed: `steele2014splittable` (Steele, Lea, Flood, OOPSLA 2014) was added to refs.bib at line 268 and is cited in `predictability-fragment.tex:20`. The PSR sub-item stands on the D6 alternative the round-1 audit accepted (definition by the kernel's stated sampling model rather than a Bailey and Lopez de Prado 2012 citation); no 2012 PSR entry exists in refs.bib. |
| R3 | NOT-FIXED (declared deliberate) | DECLINED | `refs.bib` companion entry still carries a GitHub URL, "Preprint", a version note and no arXiv id or DOI. Disclosure check: the version note is present and now reads 0.9.0; the absence of a DOI is a locator-quality issue with nothing to disclose. |
| R4 | NOT-FIXED | CLOSED | `jerome2023mbtgym` title corrected to `{mbt-gym}: Reinforcement Learning for Model-Based Limit Order Book Trading` (brace-protected). The `eprint` / `archivePrefix` fields were stripped from all seven `@inproceedings` entries that carried them. |

### Round-2 new issues

| ID | R3 verdict | Evidence |
|---|---|---|
| N1 (preamble count garble) | CLOSED | See S1. "eight core calibrations plus two follow-up probes" followed by a list of three is gone; the replacement counts 10 findings and 15 subsections and both numbers are correct against the file. |
| N2 (stale Appendix A witness paragraph) | CLOSED | The witness paragraph in `A-commands.tex` now reads "The whole procedure is repeated under five independent noise paths per cell, with noise-seed uniqueness asserted in the producer; the first path is serialized as the primary witness and all five are serialized under `noise_replicates`, which is what tab:witness summarizes." That matches the shipped `make-witness.py` and the serialized structure. |
| N3 (Appendix A missing artifacts) | CLOSED | The F4 paragraph now names `paper/figures/f4-calm-calibration.pdf` and the `calm_calibration` key; the F6 paragraph now names `paper/figures/f6-endogenous.pdf` and the `endogenous` key. |
| N4 (ten vs 50 unit mismatch) | CLOSED | Abstract, intro and README now all read "at every attained crossing of all ten eligible cells, replicated over five independent noise paths each", which reconciles with the fragment's "all 50 attained crossings" (10 cells x 5 paths). |
| N5 (stale README witness number, unlocatable bp figures) | CLOSED | README now reads "A turnover-rationing variant does pass, from a mean signal-truth correlation of 0.848 over five noise paths (range [0.777, 0.930])". Recomputed from `witness.json` `noise_replicates`: deadband_hold, gap-band, Calm gives mean 0.8478, min 0.7766, max 0.9297. The bp figures are now locatable and correct: sign_follow gap-band Calm has `mean_return` -0.0014370504 at s=0 (14.4 bp/bar) and +0.00026533 at s=1 (2.7 bp/bar) with `n_seeds_passing` 11, which is the stated "5 of 16 seeds short of the PSR gate". All 12 table cells recompute exactly. |
| N6 (three granularities of the same count) | CLOSED | The positive-control fragment now separates them explicitly: "Three granularities of the same count are worth separating: of the 135 cells, four have familywise-positive intervals (the 9:1 and 8:2 uniform schedules on each of the two no-follower arms, +21.8, +10.9, +18.4 and +8.9e-4); they occupy two arms; and the two reported winners are the 9:1 cell on each arm." Recomputed independently over all 135 stored cells at t = 6.391202695754376: exactly four familywise-positive, at theory 9:1 (+21.8 [10.0, 33.6]), theory 8:2 (+10.9 [2.1, 19.7]), temporary_impact 9:1 (+18.4 [6.4, 30.4]) and temporary_impact 8:2 (+8.9 [0.04, 17.9]). Both arms carry `follower_gain: 0.0`, so "no-follower" is right. Canonical arm has zero pointwise-positive cells; all 45 linear-exponent cells have negative means; no positive mean at beta = 0.7. See X-3b on the fourth cell's lower bound. |
| N7 (noun drift) | CLOSED | See S5. |

### Specific verifications requested

| # | Check | Result |
|---|---|---|
| 1 | M-14: no site classifies an F3 cell by the single 0.34 gap; caption and body match `f3-generalization.json` | PASS. All six quoted intervals match to 3 decimal places (one truncation, 0.9995 printed 0.999). |
| 2 | A1: read and run `test_python_golden.py`; seven goldens match `scenario_gen.rs`; `04-contract.tex` claims exactly what the test proves | PASS. 8 tests passed. All seven constants byte-equal. The contract sentence is scoped to "the scenario bytes returned through the Python bindings", which is precisely the covered surface. One wording overclaim: X-2. |
| 3 | Findings map: count subsections and follow-ups | PASS. 15 subsections, 10 findings, 5 follow-up subsections, calm-tails correctly described as a paragraph. Placement rule verified against `\input` order. |
| 4 | README numbers: witness threshold 0.848 [0.777, 0.930] and the market-making bp figures, recomputed from `witness.json` and `f2-regret.json` | PASS. 0.8478 / 0.7766 / 0.9297 for the witness cell; 14.37 and 2.65 bp/bar with 11/16 passing for the sign follower. F2 README figures 84.6, 0.037, [-8.7, 8.8] and 58.8 all match `f2-regret.json` (84.6358, 0.037376, [-8.7279, 8.8026], 58.8045). |
| 5 | Positive-control granularity sentences against the four familywise-positive cells; Bonferroni constants recomputed | PASS. Four cells, values and arms as stated. Recomputed t(1 - 0.05/270, 7) = 6.391202695754376 against source `BONFERRONI_T_CRIT_DF7 = 6.391202695754376` (exact). Recomputed t(1 - 0.05/390, 7) = 6.786175237491603 against source `BONFERRONI_T_CRIT_DF7_GLOBAL = 6.7861752374916025` (agree to 1 ulp). Source asserts `EXTENDED_FAMILY_SIZE == 60` and `GLOBAL_MANIPULATION_FAMILY_SIZE == 195`. Extension counts recomputed over all 60 stored cells at the global constant: 23 positive, 31 negative, 6 crossing; largest cell is the 45:5 theory trip at +135.0e-4 with familywise [122.1, 148.0]e-4. All match the fragment, the abstract and the README. |
| 6 | Cross-document agreement of every shared number; companion citation version against what SharpeBench actually released | MOSTLY PASS, two exceptions. Every shared quantitative claim checked across abstract, intro, 03-env, 05 preamble, F1 to F8, the six fragments, limitations, Appendix A and README agrees: 23 of 24 / 1 of 24, 0/8-0/8-1/8, 0/8-8/8-4/8 clustered, 0.75 best pass^k, 0.1114 and 0.0243, 84.6 / 0.037 / 36.6 / 58.8, 0.877 and 0.973, +21.8 [17.4, 26.2] [10.0, 33.6], +26.3 [23.7, 28.9], 23/31/6, +135.0 [122.1, 148.0], +0.095 to -0.196, 16/16 and 0/16, 3.6e-15 and 5.7e-14, 8.6 million CPU-years, 0.848 [0.777, 0.930], 1 of 8. Exceptions: the companion citation is stale (X-5) and the CHANGELOG leg is UNVERIFIED (below). |

## Counts

- Items in scope: 46 (17 round-2 PARTIALLY-FIXED, 17 non-declined non-skipped NOT-FIXED, 7 round-2 NEW, 5 DECLINED).
- CLOSED: 39
- STILL-OPEN: 2 (W2, W3)
- REGRESSED: 0
- DECLINED-WITH-REASON: 5 (M-05, C7, R3, W5, W6)
- SKIPPED per operator: 1 (W4, abstract length)
- UNVERIFIED sub-claims: 2 (M-15's "reproduces 0.1114 exactly" rescoring; the CHANGELOG leg)
- New issues found in this round: 6 (1 major, 5 minor)

Every MAJOR the round-1 audits raised is now closed and independently confirmed. M-14, the
single unrepaired MAJOR at round 2, is closed. A1, the one remaining contradiction, is
closed by a test that runs.

## Residual list, with concrete fixes

1. X-1 (MAJOR, new): regenerate `paper/evidence/provenance.json` with
   `python paper/src/make-provenance.py` and commit it in the same change as any future
   evidence, figure, section or producer edit. Separately, tighten `source_snapshot_scope`
   so `crates/**/*.rs` cannot match `crates/*/target/**`.
2. X-5: bump the `refs.bib` companion note from Version 0.9.0 to Version 0.10.0, or state
   in `A-commands.tex` which companion release the evidence was scored against and why the
   citation pins an earlier one.
3. X-2: soften `04-contract.tex` from "committed FNV-1a goldens that all three runtimes
   assert" to something the wasm suite actually satisfies, for example "committed FNV-1a
   goldens the native and Python suites assert in full and the WebAssembly suite asserts
   for the canonical and clustered scenarios".
4. X-3: change the F1 difference half-width from 0.33 to 0.35, or say the 0.33 is computed
   at the observed Calm and Hard rates rather than at p = 1/2.
5. X-3b: add a clause to the positive-control fragment noting that the fourth
   familywise-positive cell (temporary_impact 8:2) has a lower bound of +0.04e-4, that is,
   it clears zero only marginally.
6. X-4: rename the exported `analytically_optimal_policy` to `closed_form_reference_policy`
   with the old name retained as a deprecated alias, so the public symbol matches the
   corrected docstrings.
7. W2: replace "the honest zero" in F1 and limitations with "the corrected zero" or "the
   true zero", leaving the technical "honest adversary" label intact.
8. W3: cut "is worth stating" (02-principles) and rewrite "The minimum deserves its
   interval" (F2) as "The minimum is reported with its interval".
9. M-15 sub-claim: run the six-trial rescoring of the committed F3 return series and either
   confirm 0.1114 or restate the sentence.
10. M-05 (declined): if the decline stands, add half a sentence to 03-env noting the
    timescale-asymmetry coarse-graining uses zero-padded leading windows, so the decline is
    disclosed rather than silent.

## New issues introduced or discovered in this round

- X-1 (MAJOR, discovered this round, not raised by either audit or round 2): the provenance
  manifest does not validate against the tree it claims to pin. `A-commands.tex` states
  that the manifest records "the SHA-256 digest of every JSON and figure artifact" and that
  "the content snapshot, rather than a package label, identifies the exact source state
  every number in this paper was produced from". Recomputed SHA-256 over the working tree
  at HEAD: 6 of 28 artifact digests and 30 of 116 source-file digests do not match. The
  mismatched artifacts are `witness.json` (which `c4b5785` grew by 13,065 lines, adding the
  entire `noise_replicates` block that `tab:witness` now reports; it was absent from the
  committed file at `b750c51`) plus `f4-calm-calibration.pdf`, `f5-boundaries.pdf`,
  `f5-size-response.pdf`, `predictability.pdf` and `witness.pdf`. The mismatched source
  files include all 18 section and fragment `.tex` files touched by the closure, `refs.bib`,
  `lib.rs` and `market_making.py`. Attribution is mixed: at `a1c24a6`, the commit
  immediately before the closure, the manifest was already stale at 4 artifacts and 15
  source files, so this is a pre-existing defect that `c4b5785` worsened rather than
  introduced. `provenance.json` was last regenerated at `b750c51` and `repository_base_head`
  is `fa494389`, five commits behind HEAD. Related hygiene bug: `source_snapshot_scope`'s
  `crates/**/*.rs` glob has pulled machine-local build outputs under
  `crates/sharpearena-py/target/debug/build/**/out/*.rs` into the "source snapshot", so the
  manifest is not reproducible across machines even when regenerated.
- X-2 (minor, introduced by the closure): `04-contract.tex` says the surfaces are "pinned to
  the byte by committed FNV-1a goldens that all three runtimes assert". The native suite
  asserts all seven goldens and the new Python suite asserts all seven, but
  `crates/sharpearena-wasm/src/lib.rs` asserts only two (the calm scenario at
  `0xb7cf_976c_7121_9c52` and the clustered variant). The new test file's own docstring is
  accurate on this point ("The wasm crate asserts the calm and clustered values"), so the
  contract section overstates what the test docstring concedes. Related and pre-existing:
  the wasm crate's golden test is a Rust `#[test]` executed on the host, and CI only runs
  `cargo build -p sharpearena-wasm --target wasm32-unknown-unknown`, so no golden is
  asserted by a binary actually executing on wasm32.
- X-3 (minor, introduced by the closure): the F1 binomial clause states a single-rate
  half-width "of about 0.24" at "a rate of one half", which is correct (0.245), then a
  difference half-width "about 0.33". At p = 1/2 and n = 16 the difference half-width is
  1.96 * sqrt(2 * 0.25 / 16) = 0.35. The 0.33 is what one gets from the observed Calm and
  Hard rates (0.62 and 0.31), which is a different convention than the sentence declares.
  No conclusion changes: the Calm-to-Extreme drop of 0.56 clears either threshold and the
  other two steps clear neither.
- X-3b (minor, introduced by the closure): the positive-control fragment now lists four
  familywise-positive cells without noting that one of them is at the boundary. The
  temporary_impact 8:2 cell's global-familywise lower bound is +0.038e-4, that is, four
  hundredths of the units the sentence reports to one decimal. "Four have
  familywise-positive intervals" is true but fragile, and a reader recomputing it at a
  slightly different critical value would get three.
- X-4 (minor, residual of the M-02 fix): the docstrings in `market_making.py` were corrected
  throughout, but the exported symbol is still `analytically_optimal_policy` (it remains in
  `__all__`, and the internal helper is still `_optimal_depths`). The name a user reads at
  the call site still asserts the optimality claim the prose now retracts.
- X-5 (minor, and stale as of this review): the closure moved the companion citation to
  "Version 0.9.0", but the sibling repository has since released v0.10.0 (tag `v0.10.0`
  present, workspace `Cargo.toml` at `version = "0.10.0"`, HEAD `b9cfabd release: v0.10.0`).
  The citation is one release behind. This compounds the version spread C7 already
  described: the paper simultaneously cites the companion at 0.9.0, records SharpeBench 0.5.0
  in the evidence where a runtime version is serialized, and discusses "the pre-0.5.0
  kernel", while the sharpearena package's own F1 artifact serializes 0.9.0, so two
  unrelated 0.9.0s now appear in Appendix A and refs.bib.

## Unverified

- The CHANGELOG leg of check 6 is UNVERIFIED. What remains to check: whether the
  `[Unreleased]` section and its `compare/v0.13.0...HEAD` link are stale against the v0.14.0
  and v0.15.0 tags, and whether `c4b5785` has any CHANGELOG entry at all (the v0.13.0
  section still records "the companion citation reads SharpeBench version 0.8.0" with no
  visible superseding entry for the 0.9.0 bump).
- M-15's sub-claim that rescoring the committed F3 return series at six declared trials
  reproduces 0.1114 exactly. What remains to check: drive the SharpeBench `score_run` kernel
  over `f3-generalization.json`'s `per_seed_returns` for the Hard train band with
  `n_trials = 6` and compare to 0.1114.

## Bottom line

The manuscript's prose is internally consistent with its committed evidence on every
quantitative claim this review recomputed, but the repository's own provenance manifest is
not: 6 of 28 artifact digests and 30 of 116 source digests no longer match the files they
pin, so the paper's advertised content-hash guarantee does not currently hold as committed.
